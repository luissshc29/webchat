export type LoginDataType = {
  email: string;
  password: string;
};

export type RegisterDataType = {
  name: string;
  username: string;
  newEmail: string;
  avatarUrl: string;
  newPassword: string;
  repeatPassword: string;
};
